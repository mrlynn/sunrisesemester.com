## Progress

### Working
- Public site pages and admin area routes.
- MongoDB Atlas connection helper `src/lib/mongodb.js`.
- Admin auth via `ss_admin` cookie and middleware protection.

### In progress
- Weekly crossword feature (puzzle storage, user runs, UI, leaderboard).

### Known issues / notes
- `@jaredreisinger/react-crossword` has a React 18 peer dependency; installed with `--legacy-peer-deps` under React 19. Build passes.

